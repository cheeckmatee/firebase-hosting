name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main  # تشغيل النشر عند كل تحديث على الفرع الرئيسي

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Hosting
        run: firebase deploy --token ${{1//03OoABh2KFslBCgYIARAAGAMSNwF-L9IrVVn7pohQvkry4BVWYGFcIMgSLY6JSAQLBwsbfrOyR2oKynTMTDOJKbH2GAADao_wXZM}}
